contract_id: week3-document-refinery-extractions
snapshot_timestamp: '2026-04-01T21:27:49Z'
snapshot_filename_ts: 20260401T212749Z
schema:
  doc_id:
    type: string
    format: uuid
    pattern: ^[0-9a-f-]{36}$
    required: true
    unique: true
    description: Primary key (deterministic UUID post-migration).
  source_path:
    type: string
    required: true
    description: Source document path or filename.
  source_hash:
    type: string
    pattern: ^[a-f0-9]{64}$
    required: false
    description: SHA-256 when present; often null in legacy migrated export.
  extracted_facts:
    type: array
    description: Facts array; migrated export uses legacy summary fact with confidence.
    items:
      confidence:
        type: number
        minimum: 0.0
        maximum: 1.0
        required: true
      fact_id:
        type: string
        format: uuid
        pattern: ^[0-9a-f-]{36}$
        required: true
  entities:
    type: array
    required: false
  extraction_model:
    type: string
    required: false
    description: Model id when present.
  processing_time_ms:
    type: integer
    minimum: 1
    required: true
  token_count:
    type: object
    properties:
      input:
        type: integer
        required: false
      output:
        type: integer
        required: false
  extracted_at:
    type: string
    format: date-time
    required: true
